/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        remotePatterns : [
            {
                hostname : "eaqbtuxoslgaylhiwkqx.supabase.co"
            }
        ]
    }
};

export default nextConfig;
